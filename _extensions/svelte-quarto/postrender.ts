// now we have a list of svelte components to compile
// (with potential duplicates) in .quarto/.svelte-quarto-matches.

// we need to compile them to .js files and get them to expected place in
// the output folder
